Further study shows that the ${**}Tool Host${**}
can be extended to support right clicking.

Wrapping the upgrade as a peripheral allows you to
right click with it.

You can control duration and if the turtle is sneaking
by passing additional arguments.
